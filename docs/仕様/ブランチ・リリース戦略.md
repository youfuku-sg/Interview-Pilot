# ブランチ・リリース戦略

## 1. 目的

この文書は、Interview-Pilot のブランチ運用、バージョン管理、リリース作成、GitHub Actions によるビルド成果物公開の方針を整理するための資料である。

このプロジェクトは当面個人利用を前提とするため、過度に複雑にしない運用を目指す。

## 2. 前提

- 当面は開発者本人の個人利用を前提とする
- 第三者配布、SaaS 化、商用提供は現時点では想定しない
- ただし、自分がインストールしやすいように GitHub Release へビルド成果物を添付したい
- GitHub Actions で Windows 向けインストーラを作成することを優先する
- macOS / Linux の成果物は、必要になった段階で対象に含める
- Pluely 由来の workflow や設定には、ライセンス、署名、updater、外部API secret 依存が残っている可能性がある

## 3. ブランチ戦略

### 3.1 基本方針

以下のブランチ構成を候補とする。

- `main`: 安定ブランチ
- `feature/<name>`: 通常開発ブランチ
- `release/v<version>`: リリース準備ブランチ
- `hotfix/v<version>`: 緊急修正ブランチ

`develop` は使わない。

### 3.2 現状との差分

現時点のローカルブランチは `master` である。

そのため、最初に以下を決める必要がある。

- `master` をそのまま安定ブランチとして使うか
- `main` を新設して `main` 運用へ寄せるか
- GitHub 側の default branch を変更するか
- 既存 GitHub Actions の trigger を `master` / `main` / tag のどれに合わせるか

推奨案:

- 今後の資料や運用は `main` を安定ブランチとして扱う
- 実際に運用へ入る前に `master` から `main` を作成する
- GitHub の default branch も `main` に変更する

ただし、これはまだ決定ではない。

### 3.3 feature ブランチ

通常の作業は `feature/<name>` で行う。

例:

- `feature/japanese-ui`
- `feature/personal-docs`
- `feature/release-workflow`
- `feature/local-llm`
- `feature/local-stt`

方針:

- 安定ブランチから分岐する
- 1つの feature は、できるだけ1つの目的に絞る
- feature 完了後、直接安定ブランチへマージせず、必要に応じて `release/v<version>` を切る
- 個人利用前提のため厳密なレビュー運用は必須ではないが、変更内容は小さく保つ

### 3.4 release ブランチ

リリース準備には `release/v<version>` を使う。

例:

- `release/v0.1.10`
- `release/v0.2.0`

方針:

- 対応する `feature/*` から分岐する
- `1 release = 1 feature` を基本とする
- 複数 feature をまとめたバッチリリースは避ける
- リリース準備ブランチでは、バージョン更新、CHANGELOG 更新、ビルド確認を行う
- 安定ブランチへマージ後、`release/*` は削除する

### 3.5 hotfix ブランチ

緊急修正には `hotfix/v<version>` を使う。

例:

- `hotfix/v0.1.11`

方針:

- 安定ブランチから分岐する
- 緊急修正の場合は、feature ブランチを別途作らず `hotfix/*` 上で直接作業してよい
- バージョンは patch を +1 する
- 安定ブランチへマージ後、`hotfix/*` は削除する

## 4. バージョン戦略

### 4.1 バージョン形式

semver 形式を使う。

形式:

```text
MAJOR.MINOR.PATCH
```

例:

- `0.1.9`
- `0.1.10`
- `0.2.0`
- `1.0.0`

### 4.2 VERSION ファイル

`VERSION` ファイルをリポジトリルートに新設する案を採用候補とする。

目的:

- 現在のリリースバージョンを単一のファイルで確認できる
- GitHub Actions やリリース作成時に参照しやすい
- `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` の version と同期する基準にできる

未決事項:

- `VERSION` を唯一の正とするか
- 既存の `package.json` / `Cargo.toml` / `tauri.conf.json` の version を手動同期するか
- 同期スクリプトを用意するか

### 4.3 CHANGELOG

`CHANGELOG.md` をリポジトリルートに新設する案を採用候補とする。

目的:

- どのリリースで何を変更したかを残す
- GitHub Release の本文を作る材料にする
- 個人利用でも、どのビルドが何のためのものか分かるようにする

### 4.4 バージョン採番

このプロジェクトでは、以下を候補とする。

候補A: 手動採番

- 小規模で分かりやすい
- 個人利用と相性がよい
- ただし、採番ミスが起きる可能性がある

候補B: Conventional Commits による自動採番

- `feat:` は minor
- `fix:` は patch
- `BREAKING CHANGE` または `!` は major
- 厳密な履歴管理に向く
- ただし、運用が少し重くなる

現時点の推奨:

- 最初は手動採番で始める
- リリース回数が増えたら自動採番を検討する

## 5. リリース戦略

### 5.1 リリースの目的

このプロジェクトにおけるリリースは、第三者配布ではなく、自分がインストールして動作確認するためのビルド成果物を作ることを主目的とする。

主な成果物:

- Windows インストーラ
- 必要に応じて macOS / Linux 用パッケージ

### 5.2 GitHub Release の扱い

GitHub Release に GitHub Actions で生成した成果物を添付する。

候補:

- draft release として作成する
- prerelease として作成する
- 通常 release として作成する

現時点の推奨:

- 個人利用前提なので draft release を基本とする
- 自分が確認して問題なければ公開に切り替える

### 5.3 リリース作成タイミング

候補:

- `release/v<version>` ブランチを push したとき
- `v<version>` タグを push したとき
- GitHub Actions を手動実行したとき

現時点の推奨:

- 最初は手動実行に対応する
- 安定したら `v<version>` タグ push で自動リリースする

理由:

- まだ workflow が安定していない
- 個人利用なので、毎回完全自動である必要はない
- 失敗時の調査がしやすい

### 5.4 タグ規則

候補:

- `v0.1.10`
- `interview-pilot-v0.1.10`
- `app-v0.1.10`

現時点の推奨:

- `v<version>` を使う

理由:

- 一般的で分かりやすい
- `release/v<version>` ブランチ名とも対応しやすい
- GitHub Release と相性がよい

## 6. GitHub Actions 方針

### 6.1 既存 workflow の扱い

既存の `.github/workflows/publish.yml` は Pluely 由来であり、以下の特徴がある。

- `master` push で起動する
- macOS / Ubuntu / Windows をビルドする
- Pluely の API secret を `.env` に書き出す
- Tauri updater 用 JSON を含める
- `TAURI_SIGNING_PRIVATE_KEY` に依存する
- Release 名や本文が Pluely のまま

個人利用向けには、まず以下を整理する必要がある。

- Pluely API secret 依存を外す
- 署名 secret がなくてもビルドできる構成にする
- updater JSON を出すかどうか決める
- Release 名を Interview-Pilot 向けにする
- 起動条件を手動実行または tag push に変更する

### 6.2 対象 OS

優先度:

1. Windows
2. macOS
3. Linux

理由:

- 要求仕様では Windows デスクトップを主対象としている
- 面接中のシステム音声取得検証も Windows を優先したい
- macOS / Linux は Tauri の既存対応を維持できればよい

### 6.3 成果物

Windows:

- `.msi`
- `.exe`

macOS:

- `.dmg`

Linux:

- `.deb`
- `.rpm`
- `.AppImage`

最初の目標:

- Windows のインストーラを GitHub Release から取得できるようにする

### 6.4 署名

当面は署名なしビルドで進める候補とする。

注意点:

- Windows SmartScreen の警告が出る可能性がある
- macOS Gatekeeper の警告が出る可能性がある
- 個人利用であれば許容できる可能性が高い

将来的に第三者配布を考える場合は、コード署名を別途検討する。

## 7. リリース手順案

初期案:

1. `feature/<name>` で作業する
2. 作業がまとまったら `release/v<version>` を作る
3. `VERSION` と `CHANGELOG.md` を更新する
4. GitHub Actions を手動実行してビルドする
5. draft release に成果物を添付する
6. インストールして動作確認する
7. 問題なければ安定ブランチへマージする
8. 必要に応じて `v<version>` タグを作る
9. `release/*` ブランチを削除する

将来案:

1. `release/v<version>` を安定ブランチへマージする
2. `v<version>` タグを push する
3. GitHub Actions が自動でビルドする
4. GitHub Release に成果物を添付する

## 8. 未決事項

- 安定ブランチを `main` に切り替えるか
- 現在の `master` をいつどう扱うか
- `VERSION` を新設するか
- `CHANGELOG.md` を新設するか
- 手動採番か自動採番か
- Release は draft / prerelease / public のどれにするか
- workflow 起動条件を手動実行 / tag push / release branch push のどれにするか
- Windows だけ先にビルドするか、3OS 同時に整備するか
- Tauri updater JSON を出すか
- 署名なしで進めるか
- アプリ名をいつ Pluely から Interview-Pilot に変えるか

## 9. 変更履歴

- 2026-07-02: 初版作成
